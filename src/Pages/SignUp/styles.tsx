import styled from 'styled-components';

const Logo = styled('div')`
  && {
    align-items: center;
    display: flex;
    flex-flow: column;
  }
`;

const Title = styled('p')`
  && {
    align-items: flex-start;
    display: flex;
    flex-flow: column;
  }
`;

export { Logo, Title };
