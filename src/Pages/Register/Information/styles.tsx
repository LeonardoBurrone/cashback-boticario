import { KeyboardDatePicker } from '@material-ui/pickers';
import styled from 'styled-components';

const CBKeyboardDatePicker = styled(KeyboardDatePicker)`
  && {
    min-width: 100%;
  }
`;

export { CBKeyboardDatePicker };
